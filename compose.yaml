services:
  nginx:
    build:
      nginx/
    ports:
      - "443:443"

  mariadb:
    build:
      mariadb/
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
    env_file:
      - .env

volumes:
  db_data:
